.pif-ranking(
  ng-class="{'is-active': vm.isActive || vm.forceActive}"
  ng-click="vm.forceActive = !vm.forceActive"
  )
  img(
    ng-src="{{vm.ranking.image}}"
  )
  .row
    .col #
    .col User
    .col Score
  .row(ng-repeat='user in users | orderBy: "score"')
    .col {{ $index + 1 }}
    .col {{ user.email }}
    .col {{ user.score }}


